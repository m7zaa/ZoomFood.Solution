@{
    ViewData["Title"] = "Add a Cuisine";
    Layout = "_Layout";
}

@model ZoomFood.Models.Cuisine;
<h3>Add a new Cuisine</h3>

@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)

    <input type="submit" value="Add new cuisine" />
}

<p>@Html.ActionLink("Back to Cuisine List", "Index")</p>